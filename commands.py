def get_command(message):

  if message.content.startswith('rs!herb') or message.content.startswith('rs!herblore'):
    return 'rs!herblore'

  if message.content.startswith('rs!attack') or message.content.startswith('rs!atk'):
    return 'rs!attack'

  if message.content.startswith('rs!hp') or message.content.startswith('rs!constitution'):
    return 'rs!constitution'

  if message.content.startswith('rs!str') or message.content.startswith('rs!strenght'):
    return 'rs!strenght'

  if message.content.startswith('rs!rgd') or message.content.startswith('rs!ranged'):
    return 'rs!ranged'

  if message.content.startswith('rs!def') or message.content.startswith('rs!defence'):
    return 'rs!defence'

  if message.content.startswith('rs!magic') or message.content.startswith('rs!mage'):
    return 'rs!magic'

  if message.content.startswith('rs!summon') or message.content.startswith('rs!summoning'):
    return 'rs!summoning'

  if message.content.startswith('rs!wc') or message.content.startswith('rs!woodcuting'):
    return 'rs!woodcuting'

  if message.content.startswith('rs!fm') or message.content.startswith('rs!firemaking'):
    return 'rs!firemaking'
  
  if message.content.startswith('rs!hunt') or message.content.startswith('rs!hunter'):
    return 'rs!hunter'

  if message.content.startswith('rs!minig') or message.content.startswith('rs!mining'):
    return 'rs!mining'

  if message.content.startswith('rs!pray') or message.content.startswith('rs!prayer'):
    return 'rs!prayer'

  if message.content.startswith('rs!pray') or message.content.startswith('rs!prayer'):
    return 'rs!prayer'

  if message.content.startswith('rs!pray') or message.content.startswith('rs!prayer'):
    return 'rs!prayer'

  if message.content.startswith('rs!pray') or message.content.startswith('rs!prayer'):
    return 'rs!prayer'

  if message.content.startswith('rs!pray') or message.content.startswith('rs!prayer'):
    return 'rs!prayer'

  if message.content.startswith('rs!magic') or message.content.startswith('rs!mage'):
    return 'rs!magic'

  if message.content.startswith('rs!summon') or message.content.startswith('rs!summoning'):
    return 'rs!summoning'

  if message.content.startswith('rs!pray') or message.content.startswith('rs!prayer'):
    return 'rs!prayer'

  if message.content.startswith('rs!pray') or message.content.startswith('rs!prayer'):
    return 'rs!prayer'
  
  if message.content.startswith('rs!pray') or message.content.startswith('rs!prayer'):
    return 'rs!prayer'

  if message.content.startswith('rs!pray') or message.content.startswith('rs!prayer'):
    return 'rs!prayer'

  if message.content.startswith('rs!pray') or message.content.startswith('rs!prayer'):
    return 'rs!prayer'

  if message.content.startswith('rs!pray') or message.content.startswith('rs!prayer'):
    return 'rs!prayer'

  if message.content.startswith('rs!pray') or message.content.startswith('rs!prayer'):
    return 'rs!prayer'

  if message.content.startswith('rs!pray') or message.content.startswith('rs!prayer'):
    return 'rs!prayer'

  if message.content.startswith('rs!pray') or message.content.startswith('rs!prayer'):
    return 'rs!prayer'